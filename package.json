{
  "name": "azure-pipeline-simple",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "ci-test": "CI=true npm test",
    "ci-build": "CI=true npm run build",
    "scan-code-dev": "./scripts/scan-code/scan-dev.sh",
    "scan-code-ci": "./scripts/scan-code/scan-ci.sh",
    "netlify-deploy": "./scripts/netlify/deploy-ci.sh",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
